# 🤖 PythonTraderBot Control Center — راهنمای شروع (فارسی)

این پکیج شامل **همهٔ کد ربات‌های معاملاتی ریپازیتوری PythonTraderBot + یک اینترفیس کامل Streamlit** برای کنترل اجرای ربات‌ها و بک‌تست‌ها است.

---

## 🚀 راه‌اندازی سریع

### مرحله ۱ — نصب پایتون
پایتون **3.12** را نصب کنید (نسخهٔ 3.12 به بالا لازم است چون کتابخانهٔ `pandas-ta` فقط برای آن منتشر می‌شود).
در نصب، گزینهٔ **Add python.exe to PATH** را فعال کنید.

### مرحله ۲ — نصب وابستگی‌ها
در پوشهٔ اصلی (جایی که این فایل هست) ترمینال را باز کنید:

```bash
pip install -r requirements.txt
```

> 💡 اگر از لینوکس/مک استفاده می‌کنید، پکیج `MetaTrader5` به‌صورت خودکار نادیده گرفته می‌شود.

### مرحله ۳ — اجرای اینترفیس

```bash
streamlit run dashboard/app.py
```

مرورگر روی `http://localhost:8501` باز می‌شود.

### مرحله ۴ — برای ربات‌های متاتریدر (فقط ویندوز)
1. ترمینال **MetaTrader 5** را نصب و اجرا کنید
2. به حساب خود لاگین کنید (**قویاً توصیه می‌شود اول حساب دمو!**)
3. در اینترفیس، صفحهٔ «🤖 ربات‌های زنده» → دکمهٔ اجرای ربات

---

## 📺 امکانات اینترفیس

| صفحه | کاربرد |
|---|---|
| 🏠 داشبورد | نمای کلی + آمارهای اعلام‌شدهٔ نویسنده |
| 🤖 ربات‌های زنده | اجرا/توقف ۱۴ ربات + لاگ زندهٔ هرکدام |
| 📈 بک‌تست | ۶ بک‌تست: مارکوف، لوریج SPY→UPRO، اسکالپر HA_RSI، بهینه‌ساز SMA، مایکل هریس، SP2L پیشرفته |
| 📊 آمار و وین‌ریت‌ها | جدول کامل وین‌ریت/سودهای ادعاشده با توضیح |
| 🧰 ابزارها | دریافت کندل از متاتریدر + ساخت سطوح حمایت/مقاومت + وضعیت پیکربندی |
| ⬇️ دانلود پکیج | ساخت مجدد و دانلود همین ZIP |

---

## 🤖 ربات‌ها و پیش‌نیازهایشان

| ربات | فایل | پیش‌نیاز |
|---|---|---|
| EasyBot | `code/EasyBot.py` | متاتریدر |
| TraderBot (۴ استراتژی) | `code/TraderBot.py` | متاتریدر + support.csv/resistance.csv + pandas-ta |
| BB_Full / BB_Half | `code/BB_Full.py`, `code/BB_Half.py` | متاتریدر |
| CE_ZLSMA_HA / _ATR | `code/CE_ZLSMA_HA.py` | متاتریدر + pandas-ta |
| HA_RSI_CE_EMA_Scalper | `code/HA_RSI_CE_EMA_Scalper.py` | متاتریدر |
| VWAP_BB_RSI | `code/VWAP_BB_RSI.py` | متاتریدر |
| SP2L (ساده و پیشرفته) | `code/SP2L/` | متاتریدر |
| TrailingBot | `code/TrailingBot.py` | متاتریدر |
| تحلیل اخبار (VADER/FinBERT) | `code/NewsSentimentClassifier/` | فقط اینترنت |

> 📌 برای تغییر **نماد/حجم معامله** هر ربات، دیکشنری `symbols_list` در ابتدای فایل آن ربات را ویرایش کنید.

---

## 📈 بک‌تست بدون متاتریدر

- **زنجیرهٔ مارکوف** و **لوریج SPY→UPRO**: فقط اینترنت (داده از یاهو فایننس)
- **اسکالپر HA_RSI** و **بهینه‌ساز SMA**: فایل CSV کندل آپلود کنید (ستون‌های `time, open, high, low, close`)
- ساخت CSV کندل: از صفحهٔ «ابزارها» با متاتریدر، یا خروجی دستی ترمینال

---

## ⚠️ نکات مهم

1. **این مجموعه آموزشی است** — آمارهای README ادعای نویسنده‌اند؛ با بک‌تست خودتان بسنجید
2. **اول حساب دمو** — هرگز مستقیم روی حساب واقعی اجرا نکنید
3. **باگ‌های شناخته‌شده** (در کد اصلی نویسنده):
   - در `TraderBot.py` و `CE_ZLSMA_HA_ATR.py` مقادیر ATR بدون `stopLossWithAtr=True` به `Meta.run` پاس می‌شوند
   - آستانه‌های عددی BB_Full برای بیت‌کوین هاردکد شده‌اند
   - زمان‌بندی ۴ساعته با فرض تایم‌زون سرور روسیه است و با تغییر DST جابجا می‌شود
   - در نسخهٔ کپی‌شدهٔ `code/Meta.py` دو باگ کوچک (فراخوانی TeleBot و امضای TrailingStopLoss) اصلاح شده است
4. **کلیدهای API** (فایل `access_id_secret_key.csv` برای CoinEx) را هرگز کامیت/اشتراک نگذارید
5. برای اعلان تلگرام: توکن و chatId را در `code/TelegramBot.py` پر کنید و `Meta.teleBotMessage = True` بگذارید

---

## 🗂️ ساختار پکیج

```
PythonTraderBot_ControlCenter/
├── START_HERE_FA.md          ← همین راهنما
├── requirements.txt
├── code/                     ← کد اصلی ربات‌ها + بک‌تست‌های آماده
│   ├── Meta.py               ← لایهٔ اجرای متاتریدر (کپی برای اجرای مستقیم)
│   ├── run_michael_harris_backtest.py
│   ├── SP2L/run_sp2l_backtest.py
│   └── ...
└── dashboard/                ← اینترفیس Streamlit
    ├── app.py                ← اجرا: streamlit run dashboard/app.py
    ├── engines.py            ← موتورهای بک‌تست داخلی
    ├── catalog.py            ← کاتالوگ ربات‌ها و آمارها
    └── ...
```

📺 آموزش‌های ویدیویی نویسندهٔ اصلی: https://youtube.com/@alirezasadabadi
